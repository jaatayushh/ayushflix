# input.conf — CloudStream Desktop
# Override mpv's default right-click (pause) to open the ModernZ context menu
# The select/menu script is bundled inside ModernZ as a dependency
MBTN_RIGHT   script-binding select/menu
MBTN_RIGHT_DBL  ignore
